import { test } from 'node:test';

// PLACEHOLDER — TA-12 (Test-Author) replaces this with the real contract suite
// encoding IG-12's Tests list: optional field passes, removing id/tab/name/what/
// why fails before publish, unsupported schemaVersion is flagged, live catalog
// validates against the published schema. Skipped until authored. Builders must
// not edit this file (see ledger §10).
test.skip('contract: authored by TA-12', () => {});
